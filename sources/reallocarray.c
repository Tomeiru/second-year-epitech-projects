/*
** EPITECH PROJECT, 2022
** malloc
** File description:
** reallocarray
*/

#include "alloc.h"

void *reallocarray(void *ptr, size_t nmenb, size_t size)
{
    UNUSED(ptr);
    UNUSED(nmenb);
    UNUSED(size);
    return (NULL);
}